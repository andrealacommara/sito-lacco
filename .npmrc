# eslint-plugin-jsx-a11y and eslint-plugin-react declare peer deps up to eslint v9
# but work correctly with v10 at runtime (we use fixupPluginRules for flat config compat).
# Remove this once those packages ship peer dep updates.
legacy-peer-deps=true
